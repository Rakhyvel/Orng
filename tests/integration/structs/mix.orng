// 64
struct Pair {
    a: Int
    b: &Int
}

fn main() -> Int {
    let x: [4]Int = get_array(15, 16)
    _ = x
    let z = 64
    let y: Pair = Pair(z, &z)
    y.a
}

fn get_array(a: Int, b: Int) -> [4]Int {
    [a, b, a, b]
}