// 311

// The below "fn decl" looks like an fn decl, but it's not
// It's a template, that creates new fn decls when called
fn add(const T: Type) -> Int {
    let x: T = 311
    x
}

fn main() -> Int {
    add(Int)(300, 11)
}