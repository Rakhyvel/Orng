// 372 - TODO: for closed-world, validate template with type parameters, validate args match, don't re-validate

struct Recurse[T] {
    next: ?&Recurse[T]
    value: T
}

fn main() -> Int {
    let r = Recurse[Int](.next = .none, .value = 372)
    r.value
}