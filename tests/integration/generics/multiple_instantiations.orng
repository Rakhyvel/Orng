// 366
fn Wrapper(const T: Type) -> Type {
    (value: T,)
}

fn main() -> Int {
    let x: Wrapper(Int) = (.value = 366,)
    let y: Wrapper(Bool) = (.value = true,)
    if y.value {
        x.value
    } else {
        unreachable
    }
}
