// 371
fn Either(const A: Type, const B: Type) -> Type {
    (a: A, b: B)
}

fn main() -> Int {
    let e1: Either(Int, Bool) = (.a = 1, .b = true)
    let e2: Either(Bool, Int) = (.a = false, .b = 2)
    if e1.b and not e2.a {
        371
    } else {
        unreachable
    }
}