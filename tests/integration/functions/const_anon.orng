// 345
fn main() -> Int {
    if is_even(-7) {
        unreachable
    } else {
        345
    }
}

fn is_even(n: Int) -> Bool {
    if n < 0 {
        return is_even(-n)
    }

    match n {
        0 => true
        1 => false
        _ => is_even(n - 2)
    }
}
