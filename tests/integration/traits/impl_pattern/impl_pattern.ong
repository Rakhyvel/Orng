// 365
fn Wrapper(const T: Type) -> Type {
    (value: T,)
}

impl for Wrapper(A)
with A: Type 
{
    fn get(&self) -> A {
        self.value
    }
}

fn main() -> Int {
    let x: Wrapper(Int) = (.value = 365,)
    x.>get()
}
