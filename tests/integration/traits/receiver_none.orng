// 291
trait My_Trait {
    fn d(x: Int, y: Int) -> Int
}

struct My_Type { x: Int, y: Int }

impl My_Trait for My_Type {
    fn d(x: Int, y: Int) -> Int {
        x + y
    }
}

fn main() -> Int {
    let my_val = My_Type(100, 45)
    _ = my_val
    my_val.>d(200, 91)
}