// 299

struct My_Type {
    x: Int
    y: Int
}

impl for My_Type {
    fn add(self, other: Self) -> Self {
        My_Type(self.x + other.x, self.y + other.y)
    }

    fn cab(self) -> Int { self.x + self.y }
}

fn main() -> Int {
    let mut a = My_Type(100, 90)
    a = a.>add(My_Type(100, 9))
    a.>cab()
}