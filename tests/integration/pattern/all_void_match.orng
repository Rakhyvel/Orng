// 345
fn main() -> Int {
    const My_Sum = (a | b | c)
    let x: My_Sum = My_Sum.b
    _ = x
    if @typeof(match x {
        .a => unreachable
        .b => unreachable
        .c => unreachable
    }) == Void {
        345
    } else {
        unreachable
    }
}