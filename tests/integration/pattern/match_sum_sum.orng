// 173
fn main() -> Int {
    enum A_Type { aa(Int), ab(Int) }
    enum B_Type { ba(Int), bb(Int) }
    enum The_Type { a(A_Type), b(B_Type) }
    let x: The_Type = The_Type.b(.ba(173))

    match x {
        .a(y) => match y {
            .aa(z) => z
            .ab(z) => z
            _        => unreachable
        }
        .b(y) => match y {
            .ba(z) => z
            .bb(z) => z
            _        => unreachable
        }
        _ => unreachable
    }
}
