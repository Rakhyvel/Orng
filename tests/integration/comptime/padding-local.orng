// 256
fn main() -> Int {
    let x: Int8 = 50
    let y: Int16 = 200
    let z: Int8 = 6
    f(x, y, z)
}

fn f(x: Int8, y: Int16, z: Int8) -> Int {
    if x == 50 and y == 200 and z == 6 {
        256
    } else {
        unreachable
    }
}