// typeclass_eq.orng:8:9: error: expected a value of a(n) equalable type, got `Void`
//     x == y // the ONLY type that isn't equalable
//        ^
// 
fn main() -> Bool {
    let x: Void = unreachable
    let y: Void = unreachable
    x == y // the ONLY type that isn't equalable
}