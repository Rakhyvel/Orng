// dyn_type_not_impl_trait.orng:11:8: error: the type `Int` does not implement the trait `Lol`
//     f(&x)
//       ^
// 
trait Lol {
    fn lol() -> ()
}

fn main() -> Int {
    let x = 4
    f(&x)
    4
}

fn f(x: &dyn Lol) -> () {
    _ = x
    x.>lol()
}
