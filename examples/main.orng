import std::debug
import std::ffi
import externs

fn main() -> ()!() {
    debug::println("Hello, World!")
    externs::println("Hello, World f!")
    .ok({})
}

test "this is a test!" {
    let x = 5
    _ = x
}