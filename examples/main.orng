import std::debug
import std::ffi
import externs

fn main() -> ()!() {
    debug::println("Hello, World!")
    externs::println("Hello, World f!")
    .ok({})
}

test "test of tests" {
    let x = 5
    _ = x
}

test "test that tests work" {
    let x = 5
    _ = x
}