const TokenKind =
    | identifier
    | comma

fn test: ()->T =
    if test
        test
            terst
            terst
        test
        return lol
    else
        return lmao
    return 45